#include "main.h"

/**
 * _abs - This computes the absolute value of an integer
 * @n: This is the int to check
 * Return: 0 or the absolute value of n
 */
int _abs(int n)
{
	return (n >= 0 ? n : n * -1);
}
